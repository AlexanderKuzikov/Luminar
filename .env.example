# API key and base URL for the active provider.
# If set, these override the value stored in data/config.json.
# Useful for CI, scripting, or first-run without opening the UI.

API_KEY=
API_BASE_URL=
PORT=3000
